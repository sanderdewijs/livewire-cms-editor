# Supply-chain hardening for local installs.
# Pin exact versions on `npm install` so a caret range can't silently pull a
# newer (possibly compromised) release; the committed lockfile stays the source
# of truth. Use `npm ci` for reproducible installs.
save-exact=true

# Surface advisories on install.
audit=true
